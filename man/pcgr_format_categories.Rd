% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umccrise.R
\name{pcgr_format_categories}
\alias{pcgr_format_categories}
\title{Format PCGR Selection-Filtered Categories}
\usage{
pcgr_format_categories(cats_str)
}
\arguments{
\item{cats_str}{A single \code{"tier|impact|region:count"} string, a
comma-separated string of multiple such entries, or a character vector of
entries (bolt may write this either way).}
}
\value{
A newline-separated string of formatted category lines, or \code{""}
if \code{cats_str} is empty.
}
\description{
Parses a \code{"tier|impact|region:count, ..."} string (or vector of such
entries) written by bolt (sash #52) into readable lines, e.g.
\code{"- tier N / noncoding / other: 80,321 variants"}. Malformed entries
(missing a \code{|} or \code{:} field) are skipped rather than erroring, since this
feeds a clinical report and a single bad category shouldn't abort the
whole render.
}
\examples{
(y <- pcgr_format_categories("N|intronic|difficult:80321"))
(z <- pcgr_format_categories(c("N|intronic|difficult:80321", "2|impacts_other|giab_conf:5000")))
}
